[PromptKit OS: Level 2 (Controlled) — OAuth + roles touches auth and permissions.]
This requires a canonical Task Record at docs/tasks/<task-id>.md and a spec via pk:plan and pk:auth before any implementation.
