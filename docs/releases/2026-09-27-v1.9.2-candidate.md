# PromptKit OS v1.9.2 Preliminary Candidate

- **Record Type**: `SemVer Candidate Record`
- **Evaluation ID**: `REL-2026-09-27-V1.9.2-001`
- **Candidate Status**: `preliminary`
- **Candidate Core Version**: `1.9.2`
- **Prerelease Identifier**: `N/A`
- **Display Candidate Version**: `1.9.2`
- **Greatest Effective Impact**: `patch`
- **Impact Precedence Rationale**: `The dominant effective impact is a CI/distribution correction: the publish job no longer writes an npmrc auth entry that diverted npm trusted publishing (OIDC) to a token publish, and a new preflight assertion fails the release early with an explicit diagnostic if any token auth would be used. No Public PromptKit Contract surface changes: no workflow, protocol, template, installer, directive, or documented consumer behavior changes in effect; init.sh/init.ps1 remain the single source of truth and templates/agent-directive-template.md is byte-unchanged. The CHANGELOG gains a Failed entry recording the v1.9.1 E404 and a Fixed entry describing the correction. Because nothing a consumer can observe changes - v1.9.0 remains the published courier and stays valid - the greatest effective impact is patch. This is a corrective patch to a failed release, not a feature or contract change.`
- **Supporting Eligible Commits**: `ed1c448 (fix(ci): stop npmrc auth entry from diverting courier publish off OIDC (#421))`
- **Candidate Provenance**: `REL-2026-09-27-V1.9.2-001, Release Range End ed1c44869f1999fa8f808aba69718ff07842ac43`
- **First Release Candidate Rule**: `N/A`
- **Prerelease / Promotion Record**: `N/A`
- **Candidate Blockers**: `None for the code change. Two items remain human Release Coordinator actions and are tracked in issue #417: the npmjs.com trusted publisher must be confirmed for repository promptkit-os, workflow release-npm.yml, with the Environment field left empty; and the exposed granular npm token must be revoked on npmjs.com only AFTER a successful token-free publish. The v1.9.1 failed release burned that version number, so this candidate requires the v1.9.2 tag.`
- **Release Candidate Commit**: `ed1c44869f1999fa8f808aba69718ff07842ac43`
- **Migration and Upgrade Guidance**: `No breaking changes. Affected consumers: none forced, no action required. The change is confined to the repository's own release pipeline. npx consumers continue to resolve 1.9.0 until v1.9.2 publishes, and 1.9.0 remains valid and unchanged. Maintainers publishing a release must confirm the npmjs.com trusted publisher (Environment field empty) before tagging; the job now fails fast with an explicit message if NODE_AUTH_TOKEN, NPM_TOKEN, or an npmrc auth entry is present, instead of failing at the registry with a 404 after a full release run. Upgrade for consumers remains git submodule update --remote .promptkit + re-run init.sh/init.ps1 (idempotent), or the unchanged npx courier. All existing PROMPTKIT.md, DESIGN.md, docs/STATE.md, Task Records, and customizations remain valid.`
