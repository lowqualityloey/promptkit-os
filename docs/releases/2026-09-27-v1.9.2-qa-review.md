# PromptKit OS QA Review Record: REL-2026-09-27-V1.9.2-001

- **Record Type**: `QA Review Record`
- **Evaluation ID**: `REL-2026-09-27-V1.9.2-001`
- **Range Boundary Result**: `Pass`
- **Candidate Membership Result**: `Pass`
- **Eligibility and Maintenance Classification Result**: `Pass`
- **Complex-History Normalization Result**: `Pass`
- **SemVer Precedence Result**: `Pass`
- **Breaking-Guidance Result**: `N/A`
- **Every Release Note Has Supporting Contract Impact Evidence**: `Pass`
- **Every Effective User-Observable Contract Change Has Exactly One Public Release Note**: `Pass`
- **QA/Reviewer Findings**: `Field detail: range v1.9.1(02bbfc6)..RC first-parent verified; linear squash topology, no revert/duplicate anomalies. The dominant finding is corrective, not additive: v1.9.1 (run 36257849644) failed with E404 because actions/setup-node was given registry-url, which writes a userconfig .npmrc containing //registry.npmjs.org/:_authToken=${NODE_AUTH_TOKEN}; npm then selected token auth instead of OIDC even with the variable empty, so the OIDC token was minted and provenance signed (sigstore logIndex 2969109125) while the PUT itself was attempted as a token publish and rejected. The candidate removes registry-url so no auth entry is generated, and adds an early Assert No Token Auth Will Be Used step that fails on NODE_AUTH_TOKEN, NPM_TOKEN, or an NPM_CONFIG_USERCONFIG file containing an auth entry, converting a silent last-step release failure into an immediate diagnostic. That guard was exercised locally in all three states: clean environment passes (exit 0), NODE_AUTH_TOKEN set fails (exit 1), and an auth-bearing npmrc fails (exit 1). Impact precedence is patch - no consumer-observable surface changes and 1.9.0 remains the published, valid courier. The failed v1.9.1 tag and GitHub release are recorded as an immutable failed-release disposition and are not to be deleted or moved; the version number is burned and v1.9.2 supersedes it. Outstanding human items, tracked in #417 and not implementation blockers: confirm the npmjs.com trusted publisher with an empty Environment field, and revoke the exposed granular token only after a successful token-free publish. Mechanical verification executed at the candidate commit: documentation-contract suite 343/343 PASS, strict token budgets PASS (BALANCED 2254/2500, LITE 1210/1500), release-record validator 0 errors under the CI-equivalent filter, references 0 warnings, execution-control VALID across 44 records. Drafted by Implementor agent; pending coordinator countersignature.`
- **Named Release Blockers**: `None`
- **QA Review Decision**: `Deferred - mechanical results all Pass; Deferred pending Release Coordinator line-by-line approval of the decision fields in this chain before an Approved Release Record may exist`
- **Coordinator-Handoff Gate**: `No approved release record may be created from this chain until the coordinator accepts each decision field explicitly, including the failed-release disposition for v1.9.1 and the choice to supersede rather than reclaim the burned version number`
- **Review Date and Attestation**: `2026-09-27 09:25 UTC; attested by Implementor agent from executed command results; countersignature pending PromptKit maintainer at PR review`
- **Re-Review Result**: `N/A - initial review`
